import { createHash } from "node:crypto";
import { blobatar } from "blobatar/blob";
import { VERSION } from "./version.js";

const fmt = new Intl.NumberFormat("en");
export const esc = (value) => String(value ?? "").replace(/[&<>"']/g, (character) => ({
  "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;",
})[character]);

const styles = `
:root{--bg:#fff;--fg:#171717;--muted:#666;--faint:#8f8f8f;--line:#eaeaea;--soft:#fafafa;--hover:#f2f2f2;--focus:#0070f3;--danger:#b42318;font-family:Geist,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color-scheme:light}
.snippet{position:relative}.snippet .snippet-copy{position:absolute;inset-block-start:8px;inset-inline-end:8px;z-index:1;min-height:32px;padding-inline:10px;background:var(--bg);font-size:12px}.snippet+.hint#copy-status{display:none}.toast{position:fixed;z-index:10;inset-inline-end:24px;inset-block-end:24px;max-width:min(360px,calc(100% - 48px));padding:12px 14px;border:1px solid var(--line);border-radius:8px;background:var(--fg);color:var(--bg);box-shadow:0 12px 28px rgba(0,0,0,.16)}
.install{position:relative}.install .snippet+[data-copy-code],.settings-section .snippet+[data-copy-code]{position:absolute;z-index:1;inset-inline-end:32px;min-height:32px;padding-inline:10px;background:var(--bg);font-size:12px}.install .snippet+[data-copy-code]{inset-block-start:76px}.settings-section .snippet+[data-copy-code]{inset-block-start:142px}.snippet+[data-copy-code]+.hint{clear:both}
.settings-section[aria-labelledby="profile-title"]{position:relative;padding-inline-end:208px}.profile-avatar{position:absolute;inset-block-start:24px;inset-inline-end:24px;width:152px;display:grid!important;justify-items:center;align-items:start!important;gap:10px;text-align:center}.profile-avatar .avatar{width:112px!important;height:112px!important;border-radius:0!important;outline:0!important;background:transparent}.profile-avatar .hint{margin:0;font-size:12px;line-height:1.4}@media(max-width:760px){.settings-section[aria-labelledby="profile-title"]{padding-inline-end:24px}.profile-avatar{position:static;width:auto;margin:18px 0 24px;justify-items:start;text-align:start}.profile-avatar .avatar{width:64px!important;height:64px!important}}
@media(prefers-color-scheme:dark){:root{--bg:#000;--fg:#ededed;--muted:#a1a1a1;--faint:#777;--line:#2e2e2e;--soft:#111;--hover:#1a1a1a;--focus:#52a8ff;--danger:#ff6b61;color-scheme:dark}}
*{box-sizing:border-box}html{background:var(--bg);color:var(--fg);-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale}body{margin:0;min-width:320px;font-size:14px;line-height:1.5}a{color:inherit;text-decoration:none}button,input,select{font:inherit}button,a{-webkit-tap-highlight-color:transparent}:focus-visible{outline:2px solid var(--focus);outline-offset:2px;border-radius:4px}.skip{position:fixed;z-index:10;inset-block-start:8px;inset-inline-start:8px;padding:8px 12px;background:var(--fg);color:var(--bg);transform:translateY(-150%)}.skip:focus{transform:none}.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}
.shell{width:min(1120px,calc(100% - 48px));margin-inline:auto}.topbar{border-bottom:1px solid var(--line)}.topbar-inner{min-height:64px;display:flex;align-items:center;justify-content:space-between;gap:24px}.brand,.site-name{display:flex;align-items:center;gap:10px;font-weight:600}.mark{width:19px;height:19px;display:inline-flex;align-items:flex-end;justify-content:center;gap:2px;padding:4px;border:1px solid var(--line);border-radius:5px}.mark i{display:block;width:2px;background:var(--fg);border-radius:1px}.mark i:nth-child(1){height:4px}.mark i:nth-child(2){height:7px}.mark i:nth-child(3){height:10px}.slash{width:1px;height:24px;background:var(--line)}.site-name{font-weight:500}.site-dot{width:8px;height:8px;border-radius:50%;background:#45a557;box-shadow:0 0 0 3px color-mix(in srgb,#45a557 15%,transparent)}.nav{display:flex;align-items:center;gap:4px}.nav a,.link-button{min-height:36px;display:inline-flex;align-items:center;padding:0 12px;color:var(--muted);border:0;background:none;border-radius:6px;cursor:pointer}.nav a:hover,.link-button:hover{color:var(--fg);background:var(--hover)}.nav a[aria-current]{color:var(--fg);font-weight:500}.inline{display:inline;margin:0}
main{padding-block:56px 72px}.titlebar{display:flex;align-items:flex-end;justify-content:space-between;gap:24px;margin-block-end:32px}.eyebrow{color:var(--muted);margin:0 0 4px}.titlebar h1{font-size:28px;line-height:1.2;letter-spacing:-.04em;margin:0;font-weight:600}.actions{display:flex;align-items:center;gap:12px}.button{min-height:40px;display:inline-flex;align-items:center;justify-content:center;padding:0 16px;border:1px solid var(--fg);border-radius:7px;background:var(--fg);color:var(--bg);font-weight:500;cursor:pointer}.button.secondary{background:var(--bg);color:var(--fg);border-color:var(--line)}.button:hover{opacity:.86}
.panel,.card{border:1px solid var(--line);border-radius:12px;overflow:hidden;background:var(--bg)}.card{padding:24px}.site-list{list-style:none;padding:0;margin:0}.site-list li+li{border-top:1px solid var(--line)}.site-link{display:flex;align-items:center;justify-content:space-between;gap:24px;padding:20px 24px}.site-link:hover{background:var(--soft)}.site-link strong{display:block;font-size:16px}.site-link span{display:block;color:var(--muted)}.arrow{font-size:18px;color:var(--faint)}.empty-card{text-align:center;padding:64px 24px}.empty-card h2{margin:0 0 6px;font-size:18px}.empty-card p{color:var(--muted);margin:0 0 24px}
.form{display:grid;gap:20px;max-width:560px}.field{display:grid;gap:7px}.field label,.legend{font-weight:500}.field input,.field select{width:100%;min-height:44px;padding:9px 11px;color:var(--fg);background:var(--bg);border:1px solid var(--line);border-radius:7px}.hint{color:var(--muted);font-size:13px;margin:0}.error,.success{margin:0;padding:10px 12px;border:1px solid var(--line);border-radius:7px}.error{color:var(--danger);border-color:color-mix(in srgb,var(--danger) 30%,var(--line));background:color-mix(in srgb,var(--danger) 6%,var(--bg))}.success{color:#236c32;border-color:color-mix(in srgb,#45a557 35%,var(--line));background:color-mix(in srgb,#45a557 8%,var(--bg))}@media(prefers-color-scheme:dark){.success{color:#7bda8a}}.checks{display:grid;gap:10px;border:0;padding:0;margin:0}.check{display:flex;gap:9px;align-items:center}.check input{width:18px;height:18px}.auth{min-height:100vh;display:grid;place-items:center;padding:24px}.auth-box{width:min(100%,400px)}.auth-brand{display:flex;justify-content:center;align-items:center;gap:10px;margin-block-end:28px;font-weight:600}.auth .card h1{font-size:22px;letter-spacing:-.03em;margin:0 0 6px}.auth .intro{color:var(--muted);margin:0 0 24px}.auth .button{width:100%}.setup{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12px;overflow-wrap:anywhere;background:var(--soft);border:1px solid var(--line);padding:12px;border-radius:7px}
.current{display:inline-flex;align-items:center;gap:8px;color:var(--muted);font-size:13px}.current strong{color:var(--fg);font-weight:600;font-variant-numeric:tabular-nums}.periods{display:flex;border:1px solid var(--line);padding:3px;border-radius:9px;background:var(--soft)}.periods a{min-height:32px;display:inline-flex;align-items:center;padding-inline:12px;color:var(--muted);border-radius:6px}.periods a[aria-current]{color:var(--fg);background:var(--bg);box-shadow:0 1px 2px rgba(0,0,0,.08)}.metrics{display:grid;grid-template-columns:repeat(4,1fr);border-bottom:1px solid var(--line)}.metric{min-width:0;padding:20px 24px;border-inline-end:1px solid var(--line)}.metric:last-child{border-inline-end:0}.metric span{display:block;color:var(--muted);font-size:13px;margin-block-end:8px}.metric strong{font-size:24px;line-height:1;font-weight:500;letter-spacing:-.03em;font-variant-numeric:tabular-nums}.chart-wrap{padding:28px 24px 10px;color:var(--fg)}.chart{display:block;width:100%;height:auto;overflow:visible}.chart .grid path{stroke:var(--line);stroke-width:1;vector-effect:non-scaling-stroke}.chart text{fill:var(--faint);font:12px Geist,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.chart circle{fill:var(--bg);stroke:var(--fg);stroke-width:2;opacity:0}.chart:hover circle,.chart circle:focus{opacity:1}.reports{display:grid;grid-template-columns:1fr 1fr;gap:24px;margin-block-start:24px}.report{border:1px solid var(--line);border-radius:12px;min-width:0;padding:22px 24px}.report-head{display:flex;justify-content:space-between;align-items:center;margin-block-end:20px}.report h2{font-size:16px;letter-spacing:-.02em;margin:0}.report-head span{color:var(--muted);font-size:12px}.report ol{list-style:none;padding:0;margin:0}.report li+li{margin-block-start:16px}.row-label{display:flex;gap:16px;justify-content:space-between;align-items:center}.truncate{min-width:0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.value{font-variant-numeric:tabular-nums;color:var(--muted)}.meter{height:2px;background:var(--hover);margin-block-start:8px;overflow:hidden}.meter span{display:block;height:100%;background:var(--fg)}.empty{padding:56px 24px 46px;text-align:center;border-top:1px solid var(--line)}.empty h2{font-size:18px;margin:0 0 6px}.empty p,.empty-small{color:var(--muted);margin:0}.snippet{display:block;max-width:760px;margin:24px auto 0;padding:12px 14px;border:1px solid var(--line);border-radius:8px;background:var(--soft);overflow:auto;text-align:start}.snippet code{white-space:nowrap;font:12px ui-monospace,SFMono-Regular,Menlo,monospace}.empty-small{padding-block:36px;text-align:center}.install{margin-block-start:24px}.install h2{font-size:16px;margin:0 0 6px}.install p{color:var(--muted);margin:0}.install .snippet{max-width:none;margin-block-start:14px}
.users-panel{padding:0}.users-head{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:22px 24px 16px}.users-head h2{margin:0;font-size:16px;letter-spacing:-.02em}.users-head p{margin:4px 0 0;color:var(--muted);font-size:13px}.user-list{list-style:none;margin:0;padding:0}.user-record{display:flex;align-items:center;justify-content:space-between;gap:20px;padding:16px 24px;border-top:1px solid var(--line)}.user-identity{min-width:0;display:grid;gap:2px}.user-identity strong{overflow-wrap:anywhere;font-size:14px}.user-identity span{color:var(--muted);font-size:13px;overflow-wrap:anywhere}.user-access{display:flex;align-items:center;justify-content:flex-end;gap:10px;color:var(--muted);font-size:13px;text-align:end}.badge{display:inline-flex;padding:2px 7px;border:1px solid var(--line);border-radius:999px;font-size:12px;white-space:nowrap}.footer{border-top:1px solid var(--line);padding-block:22px;color:var(--muted);font-size:13px}.footer .shell{display:flex;justify-content:space-between;gap:20px}.footer strong{color:var(--fg);font-weight:500}.footer-link{color:var(--muted);text-decoration-line:underline;text-decoration-color:transparent;text-underline-offset:3px}.footer-link:hover{color:var(--fg);text-decoration-color:currentColor}
@media(max-width:760px){.shell{width:min(100% - 32px,1120px)}main{padding-block:36px 56px}.titlebar{align-items:flex-start;flex-direction:column;margin-block-end:24px}.titlebar .actions{width:100%;justify-content:space-between}.metrics{grid-template-columns:1fr 1fr}.metric:nth-child(2){border-inline-end:0}.metric:nth-child(-n+2){border-bottom:1px solid var(--line)}.reports{grid-template-columns:1fr}.site-name span:last-child{display:none}.nav .email{display:none}.field input,.field select{font-size:16px}}
@media(max-width:460px){.shell{width:calc(100% - 24px)}.brand>span:last-child,.slash{display:none}.metric{padding:18px}.metric strong{font-size:21px}.periods a{padding-inline:9px}.report{padding-inline:18px}.user-record{align-items:flex-start;flex-direction:column;gap:10px;padding:16px}.users-head{padding:18px 16px 14px}.user-access{justify-content:flex-start;text-align:start;flex-wrap:wrap}.footer .shell{flex-direction:column;gap:4px}.topbar-inner{gap:8px}.nav a,.link-button{padding-inline:8px}}
@media(prefers-reduced-motion:no-preference){.nav a,.periods a,.skip,.button,.site-link{transition:background-color .15s ease,color .15s ease,opacity .15s ease,transform .15s ease}.button:active,a:active{transform:scale(.96)}}
.site-link:active{transform:none}.site-list{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:16px}.site-list li+li{border-top:0}.site-link{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:20px;min-height:156px;padding:22px;border:1px solid var(--line);border-radius:12px;background:var(--bg);box-shadow:0 1px 2px rgba(0,0,0,.03);transition-property:background-color,border-color,box-shadow}.site-link:hover{border-color:color-mix(in srgb,var(--fg) 18%,var(--line));box-shadow:0 5px 14px rgba(0,0,0,.06)}.site-domain{margin-block-start:3px}.site-overview{align-self:end;display:grid;grid-template-columns:repeat(2,auto);gap:16px}.site-overview span{display:grid;gap:2px;color:var(--muted);font-size:12px}.site-overview strong{color:var(--fg);font-size:18px;font-variant-numeric:tabular-nums}.site-link .site-overview span{display:grid;color:var(--muted)}.site-switcher{display:flex;gap:8px}.site-switcher select{min-width:150px;min-height:40px;padding:0 10px;color:var(--fg);background:var(--bg);border:1px solid var(--line);border-radius:7px}.site-switcher button{min-height:40px;padding-inline:12px}.nav{position:relative}.account-menu summary{min-height:40px;display:flex;align-items:center;gap:9px;padding:4px 8px 4px 5px;border-radius:8px;cursor:pointer;list-style:none}.account-menu summary::-webkit-details-marker{display:none}.account-menu summary:hover,.account-menu[open] summary{background:var(--hover)}.avatar{width:30px;height:30px;border-radius:50%;outline:1px solid color-mix(in srgb,var(--fg) 12%,transparent);background:var(--soft);overflow:hidden}.avatar svg{display:block;width:100%;height:100%}.profile-avatar{display:flex;align-items:center;gap:12px;margin-block:16px 24px}.profile-avatar .avatar{width:56px;height:56px}.profile-avatar .hint{margin:0}.account-trigger-email{max-width:180px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.account-panel{position:absolute;z-index:2;inset-inline-end:0;inset-block-start:calc(100% + 8px);width:220px;padding:8px;border:1px solid var(--line);border-radius:10px;background:var(--bg);box-shadow:0 12px 28px rgba(0,0,0,.12)}.account-email{display:block;padding:8px 10px 10px;color:var(--muted);font-size:12px;overflow-wrap:anywhere}.account-panel a,.account-panel .link-button{width:100%;min-height:36px;display:flex;align-items:center;padding:0 10px;color:var(--fg);border:0;border-radius:6px;background:none;text-align:start;cursor:pointer}.account-panel a:hover,.account-panel .link-button:hover{background:var(--hover)}.account-panel form{margin:4px 0 0;padding:4px 0 0;border-top:1px solid var(--line)}.snippet-formatted code{white-space:pre;line-height:1.65}.snippet-minimal{display:none}.snippet-toggle:checked~.snippet-formatted{display:none}.snippet-toggle:checked~.snippet-minimal{display:block}.snippet-toggle-label{display:inline-flex;align-items:center;gap:8px;margin-block-start:18px;color:var(--muted);cursor:pointer}.snippet-toggle{width:16px;height:16px}.chart-guide{stroke:var(--muted);stroke-width:1;stroke-dasharray:3 3}.chart-point{fill:var(--bg);stroke:currentColor;stroke-width:2}.chart-point.is-active,.chart-point:focus{fill:currentColor}.settings-section+.settings-section{margin-block-start:24px}@media(max-width:460px){.site-switcher{width:100%}.site-switcher select{min-width:0;flex:1}.account-trigger-email{display:none}}`;

const mark = '<span class="mark" aria-hidden="true"><i></i><i></i><i></i></span>';

function displayNameFor(user) {
  return user.display_name || user.email.split("@")[0];
}

function avatarFor(name) {
  const avatarSeed = createHash("sha256").update(name).digest("hex");
  return blobatar(avatarSeed, { background: "squircle" });
}

function header(user, csrf, site) {
  const avatar = avatarFor(displayNameFor(user));
  return `<header class="topbar"><div class="shell topbar-inner"><a class="brand" href="/">${mark}<span>Risulta</span>${site ? `<span class="slash" aria-hidden="true"></span><span class="site-name"><span class="site-dot" aria-hidden="true"></span><span>${esc(site.name)}</span></span>` : ""}</a><nav class="nav" aria-label="Account"><details class="account-menu"><summary><span class="avatar" aria-hidden="true">${avatar}</span><span class="account-trigger-email">${esc(user.email)}</span></summary><div class="account-panel"><span class="account-email">${esc(user.email)}</span><a href="/">Websites</a><a href="/account">Account settings</a>${site ? `<a href="/sites/${site.id}/settings">Website settings</a>` : ""}${user.role === "admin" ? '<a href="/admin/users">Users</a>' : ""}<form method="post" action="/logout"><input type="hidden" name="csrf" value="${esc(csrf)}"><button class="link-button" type="submit">Log out</button></form></div></details></nav></div></header>`;
}

function documentPage(title, body, { user, csrf, site } = {}) {
  const hash = VERSION.match(/g([0-9a-f]+)(?:-dirty)?$/)?.[1];
  const versionUrl = VERSION.startsWith("v") && !hash
    ? `https://github.com/baronunread/risulta/releases/tag/${VERSION}`
    : hash ? `https://github.com/baronunread/risulta/commit/${hash}` : "https://github.com/baronunread/risulta";
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light dark"><meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)"><meta name="theme-color" content="#000000" media="(prefers-color-scheme: dark)"><link rel="icon" href="/favicon-light.svg" type="image/svg+xml" media="(prefers-color-scheme: light)"><link rel="icon" href="/favicon-dark.svg" type="image/svg+xml" media="(prefers-color-scheme: dark)"><link rel="manifest" href="/site.webmanifest"><title>${esc(title)} - Risulta</title><style>${styles}</style><script src="/ui.js?v=${encodeURIComponent(VERSION)}" defer></script></head><body><a class="skip" href="#main">Skip to content</a>${user ? header(user, csrf, site) : ""}${body}${user ? `<footer class="footer"><div class="shell"><span><strong>Risulta</strong> <a class="footer-link" href="${versionUrl}">${esc(VERSION)}</a></span><a class="footer-link" href="https://github.com/baronunread/risulta">View source on GitHub</a></div></footer>` : ""}</body></html>`;
}

export function loginPage({ error = "", hasUsers = true, email = "" } = {}) {
  const content = `<main class="auth" id="main"><div class="auth-box"><div class="auth-brand">${mark}<span>Risulta</span></div><section class="card" aria-labelledby="login-title"><h1 id="login-title">Sign in to Risulta</h1><p class="intro">Use one account to view all of your websites.</p>${!hasUsers ? '<p class="error">No administrator exists yet.</p><p>Set these variables and restart Risulta:</p><p class="setup">RISULTA_ADMIN_EMAIL=you@example.com<br>RISULTA_ADMIN_PASSWORD=a-long-unique-password</p>' : `<form class="form" method="post" action="/login">${error ? `<p class="error" id="login-error">${esc(error)}</p>` : ""}<div class="field"><label for="email">Email</label><input id="email" name="email" type="email" autocomplete="username" required value="${esc(email)}"${error ? ' aria-invalid="true" aria-describedby="login-error"' : ""}></div><div class="field"><label for="password">Password</label><input id="password" name="password" type="password" autocomplete="current-password" required${error ? ' aria-invalid="true" aria-describedby="login-error"' : ""}></div><button class="button" type="submit">Sign in</button></form>`}</section></div></main>`;
  return documentPage("Sign in", content);
}

export function sitesPage({ user, csrf, sites }) {
  const list = sites.length ? `<ol class="site-list">${sites.map((site) => `<li><a class="site-link" href="/sites/${site.id}"><span><strong>${esc(site.name)}</strong><span class="site-domain">${esc(site.domain)}</span></span><span class="arrow" aria-hidden="true">→</span><span class="site-overview" aria-label="Last 7 days"><span><strong>${fmt.format(site.overview.visitors)}</strong>visitors</span><span><strong>${fmt.format(site.overview.pageviews)}</strong>views</span></span></a></li>`).join("")}</ol>` : '<div class="empty-card"><h2>No websites yet</h2><p>Add your first website to start collecting private analytics.</p></div>';
  const body = `<main class="shell" id="main"><div class="titlebar"><div><p class="eyebrow">Workspace</p><h1>Your websites</h1></div>${user.role === "admin" ? '<a class="button" href="/admin/sites/new">Add website</a>' : ""}</div><section class="panel" aria-label="Websites">${list}</section></main>`;
  return documentPage("Websites", body, { user, csrf });
}

export function newSitePage({ user, csrf, error = "", values = {} }) {
  const body = `<main class="shell" id="main"><div class="titlebar"><div><p class="eyebrow">Website administration</p><h1>Add a website</h1></div></div><section class="card"><form class="form" method="post" action="/admin/sites"><input type="hidden" name="csrf" value="${esc(csrf)}">${error ? `<p class="error" id="form-error">${esc(error)}</p>` : ""}<div class="field"><label for="name">Name</label><input id="name" name="name" required maxlength="100" value="${esc(values.name)}"${error ? ' aria-describedby="form-error"' : ""}><p class="hint">A friendly name, such as Marketing site.</p></div><div class="field"><label for="domain">Domain</label><input id="domain" name="domain" required maxlength="253" inputmode="url" placeholder="example.com" value="${esc(values.domain)}"${error ? ' aria-describedby="form-error"' : ""}><p class="hint">Hostname only. Risulta rejects events claiming another domain.</p></div><div class="actions"><button class="button" type="submit">Add website</button><a class="button secondary" href="/">Cancel</a></div></form></section></main>`;
  return documentPage("Add website", body, { user, csrf });
}

export function usersPage({ user, csrf, users, sites, error = "" }) {
  const checks = sites.length ? sites.map((site) => `<label class="check"><input type="checkbox" name="site" value="${site.id}"><span>${esc(site.name)} <span class="hint">${esc(site.domain)}</span></span></label>`).join("") : '<p class="hint">Add a website before assigning a viewer.</p>';
  const records = users.map((item) => `<li class="user-record"><div class="user-identity"><strong>${esc(item.display_name || item.email)}</strong><span>${esc(item.email)}</span></div><div class="user-access"><span class="badge">${esc(item.role)}</span><span>${esc(item.role === "admin" ? "All websites" : item.sites || "No websites assigned")}</span></div></li>`).join("");
  const body = `<main class="shell" id="main"><div class="titlebar"><div><p class="eyebrow">Administration</p><h1>Users</h1></div></div><div class="reports"><section class="card"><h2>Create user</h2><form class="form" method="post" action="/admin/users"><input type="hidden" name="csrf" value="${esc(csrf)}">${error ? `<p class="error" id="form-error">${esc(error)}</p>` : ""}<div class="field"><label for="email">Email</label><input id="email" name="email" type="email" autocomplete="off" required></div><div class="field"><label for="password">Temporary password</label><input id="password" name="password" type="password" autocomplete="new-password" minlength="12" required><p class="hint">Use at least 12 characters.</p></div><div class="field"><label for="role">Access level</label><select id="role" name="role"><option value="viewer">Viewer</option><option value="admin">Administrator</option></select></div><fieldset class="checks" id="viewer-websites"><legend class="legend">Viewer websites</legend>${checks}</fieldset><button class="button" type="submit">Create user</button></form></section><section class="card users-panel" aria-labelledby="existing-users"><div class="users-head"><div><h2 id="existing-users">Existing users</h2><p>${fmt.format(users.length)} account${users.length === 1 ? "" : "s"}</p></div></div><ol class="user-list">${records}</ol></section></div></main>`;
  return documentPage("Users", body, { user, csrf });
}

export function accountPage({ user, csrf, error = "", success = "", profileError = "", profileSuccess = "", deleteError = "" }) {
  const message = error ? `<p class="error" id="password-message" role="alert">${esc(error)}</p>` : success ? `<p class="success" id="password-message" role="status">${esc(success)}</p>` : "";
  const invalid = error ? ' aria-invalid="true" aria-describedby="password-message"' : "";
  const profileMessage = profileError ? `<p class="error" id="profile-message" role="alert">${esc(profileError)}</p>` : profileSuccess ? `<p class="success" id="profile-message" role="status">${esc(profileSuccess)}</p>` : "";
  const deleteMessage = deleteError ? `<p class="error" id="delete-message" role="alert">${esc(deleteError)}</p>` : "";
  const displayName = displayNameFor(user);
  const body = `<main class="shell" id="main"><div class="titlebar"><div><p class="eyebrow">Account</p><h1>Account settings</h1></div></div><section class="card settings-section" aria-labelledby="profile-title"><h2 id="profile-title">Profile</h2><div class="profile-avatar"><img class="avatar" data-avatar-preview src="/avatar.svg?name=${encodeURIComponent(displayName)}" alt="Avatar preview"><p class="hint">Your avatar is generated from your display name.</p></div><form class="form" method="post" action="/account/profile"><input type="hidden" name="csrf" value="${esc(csrf)}">${profileMessage}<div class="field"><label for="display-name">Display name</label><input id="display-name" name="displayName" autocomplete="name" maxlength="80" required value="${esc(displayName)}"${profileError ? ' aria-invalid="true" aria-describedby="profile-message"' : ""}></div><div class="field"><label for="profile-email">Email</label><input id="profile-email" name="email" type="email" autocomplete="email" required value="${esc(user.email)}"${profileError ? ' aria-invalid="true" aria-describedby="profile-message"' : ""}></div><button class="button" type="submit">Save profile</button></form></section><section class="card settings-section" aria-labelledby="password-title"><h2 id="password-title">Change password</h2><p class="hint">Changing your password signs out your other active sessions.</p><form class="form" method="post" action="/account/password"><input type="hidden" name="csrf" value="${esc(csrf)}">${message}<div class="field"><label for="current-password">Current password</label><input id="current-password" name="currentPassword" type="password" autocomplete="current-password" required${invalid}></div><div class="field"><label for="new-password">New password</label><input id="new-password" name="newPassword" type="password" autocomplete="new-password" minlength="12" required${invalid}><p class="hint">Use at least 12 characters.</p></div><div class="field"><label for="confirm-password">Confirm new password</label><input id="confirm-password" name="confirmPassword" type="password" autocomplete="new-password" minlength="12" required${invalid}></div><button class="button" type="submit">Change password</button></form></section><section class="card settings-section" aria-labelledby="delete-title"><h2 id="delete-title">Delete account</h2><p class="hint">This permanently removes your account, active sessions, and website assignments. Shared website analytics remain available to other administrators.</p><form class="form" method="post" action="/account/delete"><input type="hidden" name="csrf" value="${esc(csrf)}">${deleteMessage}<div class="field"><label for="delete-confirmation">Type DELETE to confirm</label><input id="delete-confirmation" name="confirmation" autocomplete="off" required${deleteError ? ' aria-invalid="true" aria-describedby="delete-message"' : ""}></div><button class="button secondary" type="submit">Delete account</button></form></section></main>`;
  return documentPage("Account settings", body, { user, csrf });
}

function dateSeries(days, rows) {
  const values = new Map(rows.map((row) => [row.day, row]));
  const result = [];
  const now = new Date(); now.setUTCHours(0, 0, 0, 0);
  for (let offset = days - 1; offset >= 0; offset -= 1) {
    const date = new Date(now); date.setUTCDate(now.getUTCDate() - offset);
    const day = date.toISOString().slice(0, 10);
    result.push(values.get(day) || { day, pageviews: 0, visitors: 0 });
  }
  return result;
}

function hourSeries(rows) {
  const values = new Map(rows.map((row) => [Number(row.hour), row]));
  return Array.from({ length: 24 }, (_, hour) => ({
    hour,
    ...(values.get(hour) || { pageviews: 0, visitors: 0 }),
  }));
}

function chartLabel(point) {
  if (Number.isInteger(point.hour)) return `${String(point.hour).padStart(2, "0")}:00 UTC`;
  return new Date(`${point.day}T00:00:00Z`).toLocaleDateString("en", { month: "short", day: "numeric", timeZone: "UTC" });
}

function chart(series, visitorLabel) {
  const width = 960, height = 248, top = 16, bottom = 30;
  const max = Math.max(1, ...series.map((point) => Number(point.visitors)));
  const x = (index) => series.length === 1 ? width / 2 : index / (series.length - 1) * width;
  const y = (value) => top + (1 - Number(value) / max) * (height - top - bottom);
  const points = series.map((point, index) => `${x(index).toFixed(1)},${y(point.visitors).toFixed(1)}`).join(" ");
  const coordinates = series.map((point, index) => [x(index), y(point.visitors)]);
  const line = coordinates.map(([pointX, pointY], index) => {
    if (!index) return `M${pointX.toFixed(1)},${pointY.toFixed(1)}`;
    const [previousX, previousY] = coordinates[index - 1];
    const midpointX = (previousX + pointX) / 2;
    return `Q${previousX.toFixed(1)},${previousY.toFixed(1)} ${midpointX.toFixed(1)},${((previousY + pointY) / 2).toFixed(1)}${index === coordinates.length - 1 ? ` T${pointX.toFixed(1)},${pointY.toFixed(1)}` : ""}`;
  }).join("");
  const area = `0,${height - bottom} ${points} ${width},${height - bottom}`;
  const ticks = series.length <= 7 ? series : series.filter((_, index) => index % Math.ceil(series.length / 6) === 0 || index === series.length - 1);
  return `<svg class="chart" viewBox="0 0 ${width} ${height}" role="img" aria-labelledby="chart-title chart-desc"><title id="chart-title">${esc(visitorLabel)} over the selected period</title><desc id="chart-desc">A line chart with a peak of ${fmt.format(max)} ${esc(visitorLabel.toLowerCase())} in one interval.</desc><defs><linearGradient id="area" x1="0" x2="0" y1="0" y2="1"><stop stop-color="currentColor" stop-opacity=".16"/><stop offset="1" stop-color="currentColor" stop-opacity="0"/></linearGradient></defs><g class="grid" aria-hidden="true"><path d="M0 ${top}H${width}M0 ${(height-bottom+top)/2}H${width}M0 ${height-bottom}H${width}"/></g><polygon points="${area}" fill="url(#area)" aria-hidden="true"/><path d="${line}" fill="none" stroke="currentColor" stroke-width="2" vector-effect="non-scaling-stroke" aria-hidden="true"/><line class="chart-guide" y1="${top}" y2="${height-bottom}" hidden aria-hidden="true"/><text class="chart-tooltip" y="14" text-anchor="middle" hidden aria-hidden="true"></text>${series.map((point,index) => `<circle class="chart-point" cx="${x(index)}" cy="${y(point.visitors)}" r="5" tabindex="0" data-value="${esc(`${chartLabel(point)}: ${fmt.format(point.visitors)} ${visitorLabel.toLowerCase()}`)}"><title>${esc(chartLabel(point))}: ${fmt.format(point.visitors)} ${esc(visitorLabel.toLowerCase())}, ${fmt.format(point.pageviews)} pageviews</title></circle>`).join("")}${ticks.map((point) => { const index=series.indexOf(point); return `<text x="${x(index)}" y="${height-7}" text-anchor="${index===0?"start":index===series.length-1?"end":"middle"}">${esc(chartLabel(point).replace(" UTC", ""))}</text>`; }).join("")}</svg>`;
}

function report(title, rows, emptyLabel, visitorLabel) {
  const max = Math.max(1, ...rows.map((row) => Number(row.visitors)));
  const id = title.toLowerCase().replaceAll(" ", "-");
  return `<section class="report" aria-labelledby="${id}"><div class="report-head"><h2 id="${id}">${esc(title)}</h2><span>${esc(visitorLabel)}</span></div>${rows.length ? `<ol>${rows.map((row) => `<li><div class="row-label"><span class="truncate" title="${esc(row.label)}">${esc(row.label)}</span><span class="value">${fmt.format(row.visitors)}</span></div><div class="meter" aria-hidden="true"><span style="width:${Math.max(2,Number(row.visitors)/max*100).toFixed(1)}%"></span></div></li>`).join("")}</ol>` : `<p class="empty-small">${esc(emptyLabel)}</p>`}</section>`;
}

function goalReport(goals) {
  if (!goals.length) return "";
  return `<section class="report" aria-labelledby="goals-title"><div class="report-head"><h2 id="goals-title">Goals</h2><span>Conversions</span></div><ol>${goals.map((goal) => `<li><div class="row-label"><span class="truncate">${esc(goal.name)}</span><span class="value">${fmt.format(goal.conversions)}</span></div><p class="hint">${fmt.format(goal.unique_conversions)} unique, ${(Number(goal.conversion_rate) * 100).toFixed(1)}% conversion rate${Number(goal.value) ? `, ${fmt.format(goal.value)} value` : ""}</p></li>`).join("")}</ol></section>`;
}

function trackerSnippets(site, baseUrl) {
  const trackerUrl = `${baseUrl}/js/${site.public_key}.js`;
  const formatted = `&lt;!-- Risulta analytics --&gt;\n&lt;script\n  defer\n  src=&quot;${esc(trackerUrl)}&quot;\n&gt;&lt;/script&gt;`;
  const minimal = `&lt;script defer src=&quot;${esc(trackerUrl)}&quot;&gt;&lt;/script&gt;`;
  return { formatted, minimal };
}

function siteSwitcher(sites, currentSite) {
  if (sites.length < 2) return "";
  return `<form class="site-switcher" method="get" action="/"><label class="sr-only" for="site-switcher">Website</label><select id="site-switcher" name="site">${sites.map((site) => `<option value="${site.id}"${site.id === currentSite.id ? " selected" : ""}>${esc(site.name)}</option>`).join("")}</select><button class="button secondary" type="submit">Switch</button></form>`;
}

export function dashboardPage({ user, csrf, site, sites, analytics, days, baseUrl }) {
  const metrics = analytics.summary;
  const hasData = Number(metrics.pageviews) > 0;
  const series = days === 1 ? hourSeries(analytics.byHour) : dateSeries(days, analytics.byDay);
  const viewsPerVisit = Number(metrics.visits) ? Number(metrics.pageviews) / Number(metrics.visits) : 0;
  const visitorLabel = days === 1 ? "Unique visitors" : "Unique visitor-days";
  const { formatted } = trackerSnippets(site, baseUrl);
  const install = `<section class="card install" aria-labelledby="install-title"><h2 id="install-title">Install the tracker</h2><p>Paste this into the <code>&lt;head&gt;</code> of ${esc(site.domain)}.</p><div id="formatted-snippet" class="snippet snippet-formatted" role="region" tabindex="0" aria-label="Tracker installation code"><code>${formatted}</code></div><button class="button secondary" type="button" data-copy-code>Copy code</button><p class="hint" id="copy-status" role="status" aria-live="polite"></p></section>`;
  const body = `<main class="shell" id="main"><div class="titlebar"><div><p class="eyebrow">${esc(site.domain)}</p><h1>${days === 1 ? "Today" : `Last ${days} days`}</h1></div><div class="actions">${siteSwitcher(sites, site)}<div class="current" title="Visitors in the last 5 minutes"><span class="site-dot" aria-hidden="true"></span><strong>${fmt.format(analytics.current)}</strong> current</div><nav class="periods" aria-label="Date range">${[1,7,30].map((period) => `<a href="/sites/${site.id}?period=${period}"${period===days?' aria-current="page"':""}>${period===1?"Today":`${period}d`}</a>`).join("")}</nav></div></div><section class="panel" aria-label="Traffic summary"><div class="metrics"><div class="metric"><span>${visitorLabel}</span><strong>${fmt.format(metrics.visitors)}</strong></div><div class="metric"><span>Total visits</span><strong>${fmt.format(metrics.visits)}</strong></div><div class="metric"><span>Total pageviews</span><strong>${fmt.format(metrics.pageviews)}</strong></div><div class="metric"><span>Views per visit</span><strong>${viewsPerVisit.toFixed(2)}</strong></div></div>${hasData ? `<div class="chart-wrap">${chart(series, visitorLabel)}</div>` : '<div class="empty"><h2>Waiting for the first visitor</h2><p>Install the tracker below. This dashboard updates when you refresh.</p></div>'}</section><p class="hint metrics-note">Visitor identities reset at each UTC day. Multi-day totals are unique visitor-days, not deduplicated people.</p><div class="reports">${goalReport(analytics.goals)}${report("Top pages",analytics.paths,"Pages will appear after the first view.",visitorLabel)}${report("Top sources",analytics.referrers,"Sources will appear after the first visit.",visitorLabel)}${report("Top campaigns",analytics.campaigns,"Campaigns will appear after tagged visits.",visitorLabel)}</div>${hasData ? "" : install}</main>`;
  return documentPage(`${site.name} analytics`, body, { user, csrf, site });
}

export function settingsPage({ user, csrf, site, sites, baseUrl, goals = [], goalError = "" }) {
  const { formatted, minimal } = trackerSnippets(site, baseUrl);
  const goalSettings = user.role === "admin" ? `<section class="card settings-section" aria-labelledby="goals-settings-title"><h2 id="goals-settings-title">Goals</h2><p class="hint">Track a custom event or a pageview at one exact path.</p>${goalError ? `<p class="error" id="goal-error">${esc(goalError)}</p>` : ""}<form class="form" method="post" action="/sites/${site.id}/goals"><input type="hidden" name="csrf" value="${esc(csrf)}"><div class="field"><label for="goal-name">Goal name</label><input id="goal-name" name="name" required maxlength="100"${goalError ? ' aria-describedby="goal-error"' : ""}></div><div class="field"><label for="goal-event">Event name</label><input id="goal-event" name="eventName" required maxlength="64" placeholder="signup or pageview"${goalError ? ' aria-describedby="goal-error"' : ""}></div><div class="field"><label for="goal-path">Exact page path (optional)</label><input id="goal-path" name="path" maxlength="512" placeholder="/pricing"></div><button class="button" type="submit">Add goal</button></form>${goals.length ? `<ol class="site-list">${goals.map((goal) => `<li class="site-link"><strong>${esc(goal.name)}</strong><span>${esc(goal.event_name)}${goal.path ? ` at ${esc(goal.path)}` : ""}</span></li>`).join("")}</ol>` : ""}</section>` : "";
  const body = `<main class="shell" id="main"><div class="titlebar"><div><p class="eyebrow">${esc(site.domain)}</p><h1>Website settings</h1></div>${siteSwitcher(sites, site)}</div><section class="card settings-section" aria-labelledby="tracker-title"><h2 id="tracker-title">Tracker code</h2><p class="hint">Paste this into the <code>&lt;head&gt;</code> of ${esc(site.domain)}. It stays available here after data starts arriving.</p><label class="snippet-toggle-label"><input class="snippet-toggle" type="checkbox">Use minimal one-line snippet</label><div id="formatted-snippet" class="snippet snippet-formatted" role="region" tabindex="0" aria-label="Formatted tracker installation code"><code>${formatted}</code></div><div id="minimal-snippet" class="snippet snippet-minimal" role="region" tabindex="0" aria-label="Minimal tracker installation code"><code>${minimal}</code></div><button class="button secondary" type="button" data-copy-code>Copy code</button><p class="hint" id="copy-status" role="status" aria-live="polite"></p></section>${goalSettings}</main>`;
  return documentPage(`${site.name} settings`, body, { user, csrf, site });
}
